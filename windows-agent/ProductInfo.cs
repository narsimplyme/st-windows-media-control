namespace STMediaBridge;

internal static class ProductInfo
{
    public const string DisplayName = "ST Windows Media Control";
    // Compatibility identifiers: keep existing configuration, startup and pairing.
    public const string DataDirectory = "STMediaBridge";
}
